<UserControl x:Class="CarDealership.UI.WPF.UserControls.SellStatisticsPageControl"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:CarDealership.UI.WPF.UserControls"
             xmlns:viewModels="clr-namespace:CarDealership.UI.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="5">Целевой период: </TextBlock>
            <ComboBox Name="cboxYear" Margin="0,0,5, 0" SelectedItem="{ Binding SelectedYear}"
                      ItemsSource="{Binding YearsFilter }" Height="23" Width="120">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction CommandParameter="{Binding ElementName=cboxYear, Path=SelectedItem  }"
                                               Command="{Binding LoadStatsCommand}">
                        </i:InvokeCommandAction>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ComboBox>
            <TextBlock Margin="5">Модель авто:</TextBlock>
            <ComboBox Name="cboxCarModel" Margin="0,0,5, 0" SelectedItem="{ Binding SelectedModel }"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      ItemsSource="{Binding ModelsFilter }" Height="23" Width="120">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction CommandParameter="{Binding ElementName=cboxCarModel, Path=SelectedItem  }"
                                               Command="{Binding LoadStatsByModelCommand}">
                        </i:InvokeCommandAction>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ComboBox>
            <Button Height="23" Width="120" Content="Export to Excel"
                    Command="{Binding Path= ExportToExcelCommand}" />
            <fa:ImageAwesome Visibility="{Binding ExportProgress, Mode=OneWay}" Margin="5" Icon="Refresh" Spin="True" Height="20" Width="20"/>
            <TextBlock Margin="-5,5,5, 0">
                <Run Text="{Binding ExportResult, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>
        <fa:ImageAwesome Grid.Row="1" Visibility="{Binding LoadProgress, Mode=OneWay}" Margin="5" Icon="Refresh" Spin="True" Height="48" Width="48"/>
        <ScrollViewer Margin="10" Grid.Row="1" Padding="11" HorizontalScrollBarVisibility="Auto">
            <ScrollViewer.Template>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" MinHeight="17" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Rectangle x:Name="Corner"
                                   Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                                   Grid.Column="1" Grid.Row="0" />
                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Margin="{TemplateBinding Padding}"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}" Grid.Column="0"
                                                Grid.Row="1" CanContentScroll="{TemplateBinding CanContentScroll}"
                                                CanHorizontallyScroll="False" CanVerticallyScroll="False" />
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Cursor="Arrow"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Grid.Column="1" Grid.Row="1"
                                   AutomationProperties.AutomationId="VerticalScrollBar"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   Minimum="0"
                                   Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                   ViewportSize="{TemplateBinding ViewportHeight}" />
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Cursor="Arrow"
                                   Grid.Column="0"
                                   AutomationProperties.AutomationId="HorizontalScrollBar"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   Minimum="0"
                                   Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                   Orientation="Horizontal"
                                   ViewportSize="{TemplateBinding ViewportWidth}" />
                    </Grid>
                </ControlTemplate>
            </ScrollViewer.Template>
            <DataGrid
                Name="statsGrid"
                HorizontalAlignment="Left"
                ItemsSource="{Binding Stats, Mode=OneWay}"
                GridLinesVisibility="All"
                AutoGenerateColumns="False"
                Margin="10,10,0,0"
                VerticalAlignment="Top">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="150" Header="Модель" Binding="{Binding ModelName}" />
                    <DataGridTextColumn Header="Январь" Binding="{Binding JanuaryStats}" />
                    <DataGridTextColumn Header="Февраль" Binding="{Binding FebruaryStats}" />
                    <DataGridTextColumn Header="Март" Binding="{Binding MarchStats}" />
                    <DataGridTextColumn Header="Апрель" Binding="{Binding AprilStats}" />
                    <DataGridTextColumn Header="Май" Binding="{Binding MayStats}" />
                    <DataGridTextColumn Header="Июнь" Binding="{Binding JuneStats}" />
                    <DataGridTextColumn Header="Июль" Binding="{Binding JulyStats}" />
                    <DataGridTextColumn Header="Август" Binding="{Binding AugustStats}" />
                    <DataGridTextColumn Header="Сентябрь" Binding="{Binding SeptemberStats}" />
                    <DataGridTextColumn Header="Октябрь" Binding="{Binding OctoberStats}" />
                    <DataGridTextColumn Header="Ноябрь" Binding="{Binding NovemberStats}" />
                    <DataGridTextColumn Header="Декабрь" Binding="{Binding DecemberStats}" />
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>